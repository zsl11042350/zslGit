MyTime:TestMTime:时分秒加减器，全部化为秒后再进行处理
Student:TestStudent:实例化对象后调用toString，它返回的是字符串，直接打印即可
Student1:TestStudent1:类类型数组可以对每个数组元素实例化对象
Auto:SmallCar:TestAuto:添加无参构造有利于子类继承时不用非得用有参构造方法
Addition:BigDecimal的使用
PhoneBook:
nextInt()和nextLine()交叉，要吞掉一个回车
数组一旦定义，不要随便改变长度
字符串比较相等要用equals()
数组扩充：定义超长数组后，利用变量i在add方法内指向最后一条数据，在方法外自增1后记录真实数组大小
通过定义全局（类范围内）的数组进行对数组更改后的管控

ex04:在类中继承抽象类与实现接口
ex05:用this()在有参构造方法中调用无参构造方法；有参构造方法中已调用无参构造方法，则再传无参构造方法中有的值不会起作用；id为常量，传值是白传；可在子类中直接调用父类的方法；set变量的时机很关键，分清在构造方法之前还是之后去改变
ex07:实例方法中调用静态方法;向上转型可使返回类型一致