Test:寻找数组里的最大最小值（先默认第一个元素为最佳，再比较更换）
查找某元素（设置标志位看有没有完成某事情）
二分查找（针对有序的数组 sort() foreach）
binarySearch()   copyOf()和copyOfRange()   equals()   fill()   toString()valueOf()char[]
逆向输出一个数组（第n个和倒数第n个相等）
Ex01:双层循环后找到一个满足条件的就跳出 数组元素只与自己之后的数比较即可  记录内循环上一次变量的值
Ex02:变量有具体值可以作为数组长度  设置变量在循环中作为新数组的自增下标  
Ex03:通过循环和数组进行数值scanner和存储
Ex04:冒泡排序（最后一个数不用比故趟数0<i<arr.length-1；每走完一趟就排好一个数就少比较一次次数，故次数0<j<arr.length-1-i；arr[j]>arr[j+1]时设置中间量进行交换）
Ex05:存在连续的下标需要表示
Ex06:倒序输出